{
  "dependencies": {
    "@stripe/react-stripe-js": "^1.10.0",
    "@stripe/stripe-js": "^1.35.0",
    "concurrently": "^7.3.0"
  },
  "scripts": {
    "start": "cd server && yarn start",
    "dev": "concurrently \"cd client && yarn dev\" \"cd server && yarn dev\"",
    "prebuild": "cd server && yarn build",
    "build": "cd client && yarn build"
  }
}
